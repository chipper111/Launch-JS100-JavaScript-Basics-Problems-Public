/*
Exercise 7
12.08.22

Argument Signatures

*/

// How many arguments does the Array.prototype.join() method expect? What happens if you call it with less or more arguments?

// Calling join() with no arguments => will return a string that will by default delimit array items with a comma
// Calling join() with more than one argument => It will only take the first argument and ignore the others without return an error
